package org.microsauce.gravy.dev.gravy

import org.apache.tools.ant.Task

class Gravy extends Task {

	String projectPath
	String deployPath
	String skipTests = 'false'

	void execute() {
		Lifecycle lifecycle = new Lifecycle()
		lifecycle.deploy()
			// clean
				// compile target
				// managed libraries
			// resolve dependencies
			// install dependencies
				// install libraries
				// install modules
			// compile
			// test
			// assemble (currently deploy)
			// war
			// deploy (copy war to deployment location)
	}
}