#! /bin/sh

if [ "$GRAVY_HOME" = "" ]; then
	echo fail: GRAVY_HOME is not set.
	exit
fi
if [ "$JAVA_HOME" = "" ]; then
	echo fail: JAVA_HOME is not set.
	exit
fi

# build classpath
JARS="$GRAVY_HOME/lib/groovy/*"
for f in $JARS
do
	separator=":"
	if [ "$first" = "first" ]; then
		separator=""
		first=""
	fi

  	cp="$cp$separator$f"
done

$JAVA_HOME/bin/java -cp $cp:$GRAVY_HOME/lib/gravy.jar org.codehaus.groovy.tools.GroovyStarter --classpath $cp --main groovy.ui.GroovyMain $GRAVY_HOME/bin/scripts/bootstrap.groovy "$@"
